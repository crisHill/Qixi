package zls.com.qixi.script;

import zls.com.qixi.itf.Script;

/**
 * Created by oop on 2018/8/17.
 */

public class DirectDate extends Script{

    //private Stage stage;
    //private Human boy, girl;

    public DirectDate(){
        //stage = (Stage) Manager.getInstance().requestRole(RoleType.Stage);
        //boy = (Human) Manager.getInstance().requestRole(RoleType.Boy);
        //girl = (Human) Manager.getInstance().requestRole(RoleType.GIRL);
    }

    public void start(){
        long duration = 5000;
        //stage.move(duration, 0, -1000,0, 0);
        //boy.move(duration, (int) boy.getContentView().getX(), (int) boy.getContentView().getX() + 300, (int) boy.getContentView().getY(), (int) boy.getContentView().getY());
    }

}
